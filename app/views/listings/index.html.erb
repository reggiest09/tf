<div class="jumbotron">
  <h1>Teaching Athletes Plans For Success <br>
    </h1>
  <h2>We strive to assist our youth in identifying their strengths and develop a plan to execute their dreams.</h2>
</div>

<div class="center">
<div class="row">
  <% @listings.each do |listing| %>
  <div class="col-md-3">
    <div class="thumbnail">
     <%= link_to image_tag(listing.picture.url), listing %>
      <div class="caption">
        <h3><%= listing.name %></h3>
        <p><%= number_to_currency(listing.amount) %></p>
        <p><%= "Created by #{listing.user.name}" %></p>
        <p><%= listing.short_description %></p>
        
       <% if(user_signed_in?)&& (current_user == listing.user) %>
        <%= link_to 'Edit', edit_listing_path(listing), class: "btn-btn-link" %>
        <%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn-btn-link" %>
        <%end %>
         </div>
       </div>
     </div>
        <% end %>
      </div>
    </div>
    <br>
  
  
  

 
  
<% if user_signed_in? %>
  <%= link_to "New Listing", new_listing_path, class: "btn btn-primary", data: { no_turbolink: true } %>
<% end %>